schema_version = 1
name = "BattleTris default assets"
version = "0.1.0"

[themes.original]
path = "themes/original/theme.toml"
description = "Format v1 original theme schema with metadata, semantic tokens, layout rects, and cell atlas mapping."

[themes.high-contrast]
path = "themes/high-contrast/theme.toml"
description = "Format v1 high-contrast validation theme schema with the same semantic and atlas contract."

[sounds.generated-default]
path = "sounds/generated-default/sound-pack.toml"
description = "Semantic generated sound-pack manifest and synthesized WAV cues."
